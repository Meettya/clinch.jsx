// Generated by CoffeeScript 1.9.1

/*
This is separate React jsx addon for Clinch processor
 */

(function() {
  var React, extension, processor;

  React = require('react-tools');

  extension = '.jsx';

  processor = function(data, filename, cb) {
    var content, error;
    try {
      content = React.transform(data);
    } catch (_error) {
      error = _error;
      error.filename = filename;
      return cb(error);
    }
    return cb(null, content, true);
  };

  module.exports = {
    extension: extension,
    processor: processor
  };

}).call(this);
