// Generated by CoffeeScript 1.9.3

/*
This is separate React jsx addon for Clinch processor
 */

(function() {
  var babel, builder, extend, extension, processor;

  extend = require('whet.extend');

  babel = require('babel-core');

  extension = '.jsx';

  processor = function(data, filename, cb) {
    var error, result;
    try {
      result = babel.transform(data, {
        filename: filename,
        ast: false
      });
    } catch (_error) {
      error = _error;
      error.filename = filename;
      return cb(error);
    }
    return cb(null, result.code, true);
  };

  builder = function(raw_options) {
    return {
      extension: extension,
      processor: function(data, filename, cb) {
        var error, options, result;
        options = extend({}, raw_options, {
          filename: filename,
          ast: false
        });
        try {
          result = babel.transform(data, options);
        } catch (_error) {
          error = _error;
          error.filename = filename;
          return cb(error);
        }
        return cb(null, result.code, true);
      }
    };
  };

  builder.extension = extension;

  builder.processor = processor;

  module.exports = builder;

}).call(this);
